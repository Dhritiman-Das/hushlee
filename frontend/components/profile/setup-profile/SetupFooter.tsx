import { Button } from "@mui/material";
import React from "react";

const SetupFooter = () => {
  return (
    <div className="text-right mt-5 ">
      <Button variant="contained" className="bg-button1 text-textColor">
        Next
      </Button>
    </div>
  );
};

export default SetupFooter;
